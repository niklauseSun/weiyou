import {
    StyleSheet
} from 'react-native'

const styles = StyleSheet.create({
    body: {
        flex: 1,
        backgroundColor: '#EBEBEB'
    },
    content: {
        flex: 1,
        backgroundColor: '#fff'
    }
})

export default styles